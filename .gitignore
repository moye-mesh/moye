# Dependencies/build artifacts (regenerated by npm install / cargo build)
node_modules/
sdk/rust/target/
sdk/**/target/
*.rlib
*.so
__pycache__/
*.pyc

# Runtime data/identity keys -- must never go into the public repo
a2a/data/
a2a/orbitdb/
a2a/.ipfs_root_cid
a2a/scratch/
*.db
*.db-journal
*.db-wal
*.db-shm
*.pem
*.key
*-libp2p-key.bin

# Internal-only planning docs (ADRs/PLAN/mission tickets/identity material) -- never tracked so
# far by convention only; pinning it explicitly so a stray `git add -A` can't pull it in.
a2a/docs/

# e2e test suite (Playwright) -- dev tooling, not part of the public repo.
e2e/

# Internal smokes / dogfood mission tooling / legacy node-local UI (keep on disk, not in public mirrors)
a2a/poc/
a2a/public/
a2a/scripts/guestbook-report.js
a2a/tools/arm-room-to-*.sh
a2a/tools/watch-room-to-*.js
a2a/tools/coder-*
a2a/tools/ops-*
a2a/tools/lib/room-watch-core.js
a2a/tools/ROOM-WATCH.md

# Old a2a-only git history backup (not part of this new repo)
a2a/.git.old-a2a-only-history/

# Cloudflare CLI local cache (contains account metadata, not source code)
.wrangler/
cloudflare-worker/.wrangler/
cloudflare-pages/.wrangler/

# Logs/system files
*.log
npm-debug.log*
.DS_Store
.env
.env.local

# Local tool config + self-referencing symlinks (legacy links pointing at . itself, not project source)
.claude/
.cursor/
/moye
/www
/api/
