{
  "name": "moye-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E test suite for the moye.ai static frontend (index/directory/docs/dashboard), run against a disposable local instance of the real a2a backend",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.6.0"
  }
}
